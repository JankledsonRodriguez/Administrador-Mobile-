import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.administrador.mobile',
  appName: 'Administrador Mobile',
  webDir: 'dist'
};

export default config;
